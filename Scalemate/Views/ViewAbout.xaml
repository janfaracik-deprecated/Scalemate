<UserControl
    x:Class="Scalemate.Views.ViewAbout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Scalemate.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Scalemate.ViewModels"
    mc:Ignorable="d" Width="325" Height="400">

    <UserControl.DataContext>
        <viewModels:ViewModelAbout/>
    </UserControl.DataContext>

    <Grid>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBlock Foreground="#FF9500" Text="{Binding AppName}" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center"/>

            <TextBlock Text="{Binding Version}" FontSize="13" FontWeight="Bold" TextLineBounds="Tight" HorizontalAlignment="Center" Grid.Column="2" Opacity=".6" Margin="0,12,0,0"/>

            <Button Visibility="Collapsed">
                Rate App
            </Button>
            <Button Visibility="Collapsed">
                Share App
            </Button>

        </StackPanel>

    </Grid>

</UserControl>
